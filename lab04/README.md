# Лабораторная работа №4 — Замыкания и декораторы

Тема: Замыкания и декораторы в Python

Задание
- Реализовать замыкание, генерирующее последовательность Фибоначчи.
- Написать декоратор для кэширования (мемоизации) и показать его работу отдельно и в сочетании с замыканием.

---

## Краткая идея

- Замыкание (closure) — внутренняя функция, которая «захватывает» переменные из внешней функции и продолжает к ним доступ даже после её завершения.
- Декоратор — функция, принимающая другую функцию и возвращающая новую функцию с дополнительным поведением. В этой работе декоратор применяется для кэширования результатов.

---

## Файлы в папке `lab04`

1. `decor.py`  — реализация декоратора `cache_decorator` и простые примеры его работы (квадрат, факториал).
2. `zamik.py`  — демонстрация замыканий (счётчик, приветствие, умножитель) и отдельная секция с применением декоратора к замыканию (числа Фибоначчи).
3. `zamdek.py` — совместный пример: декоратор + замыкание для вычисления чисел Фибоначчи.
4. `png4/`     — папка для картинок/скриншотов результатов.

---

## Как запускать (примеры)

1) Пример работы только декоратора:

```bash
python3 lab04/decor.py
```

2) Пример работы только замыканий:

```bash
python3 lab04/zamik.py
```

3) Пример декоратор + замыкание (Фибоначчи):

```bash
python3 lab04/zamdek.py
```

---

## Краткое описание работы

1. В `zamik.py` и `zamdek.py` используется `memo` — словарь, который хранит уже вычисленные значения Фибоначчи (мемоизация внутри замыкания).
2. В `decor.py` декоратор `cache_decorator` поддерживает отдельный словарь `cache` и оборачивает функцию, чтобы возвращать результаты из кэша при повторных вызовах.
3. В `zamdek.py` показано совместное использование: внутренняя функция замыкания имеет свой `memo`, а декоратор добавляет внешний кэш — вместе они дают быстрые вычисления.

---

## Результаты и место для скриншотов

Вставьте сюда вывод программы и скриншоты. Например:

```
Программа запущена...
Фибоначчи(10)  = 55
Фибоначчи(35)  = 9227465
```

Добавьте изображение в `lab04/png4/` и вставьте ссылку:

![](png4/result.png)

---

## Источники

- Документация Python: https://docs.python.org/3/
- Мемоизация: https://en.wikipedia.org/wiki/Memoization

---
